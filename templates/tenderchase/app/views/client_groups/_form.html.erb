<%= form_for @client_group, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @client_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_group.errors.count, "error") %> prohibited this client_group from being saved:</h2>

      <ul>
        <% @client_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :active, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :active, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
  </div>

  
  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            client_groups_path, :class => 'btn btn-default' %>
 
<% end %>
