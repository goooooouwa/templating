<%%-
a = "<%= parent %>"
A = "<%= parent.camelize %>"
A_ = "<%= parent.capitalize %>"
as = "<%= parent.pluralize %>"

b = "<%= child %>"
B = "<%= child.camelize %>"
B_ = "<%= child.capitalize %>"
bs = "<%= child.pluralize %>"

NB = "<%= "#{namespace}::#{child}".camelize %>"

-%>